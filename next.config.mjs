/** @type {import('next').NextConfig} */
const nextConfig = {
	basePath: "/qabenchprogram",
	reactStrictMode: true,
	output: "export",
	// images: {
	// 	unoptimized: true,
	// }
};

export default nextConfig;
